#!/bin/bash

git config --global user.email "andreagalle@hotmail.it"
git config --global user.name  "andreagalle"

cd $galledanza_dir

echo ""
echo "...galledanza package `git describe --tags` loaded"
echo ""

cd $OLDPWD

